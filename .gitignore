# Rust
/target
/Cargo.lock.bak

# Models (multi-GB, fetched separately from HuggingFace)
/models

# Large benchmark fixtures (the small ones in tests/fixtures stay)
*.bin

# Editor/OS
.DS_Store
.vscode
.idea
*.swp
*.swo

# Logs / autotune cache
*.log
/autotune

# Test audio / output (local-only, regenerated)
*.wav
# Keep language-tagged fixtures tracked (force-add historically; allowlist here).
!tests/fixtures/*.wav
asr_test_output.txt

# wgpu port spike (standalone workspace + its cuBLAS reference crate)
/wgpu/target
/wgpu/cuda_ref/target

# Local-only: agent working memory, pre-freeze baseline backups
/.workbuddy
/docs/baseline/texts.bak-*
